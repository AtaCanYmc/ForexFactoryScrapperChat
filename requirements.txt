httpx
werkzeug
jinja2
defusedxml
Flask~=3.1.3
Flask-Cors~=3.0.10
pandas~=2.2.2
pytest~=9.0.3
python-dotenv~=1.2.2
pre-commit~=3.4.0
# AI/LLM integration
pydantic~=2.5.0
requests~=2.33.0
# Optional LLM providers (install as needed for your environment)
# groq>=0.4.1  # Uncomment for Groq support
# openai>=1.0.0  # Uncomment for OpenAI support
# Note: Ollama support works via requests (already installed)
